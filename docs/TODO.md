1. Publisher ⭐
формирование PublishPacket;
сериализация списка тегов и значений;
проверка размера пакета;
тесты Publisher.
2. LiveScheduler
проверка расписания 1/10/100 Гц;
работа нескольких подписок одновременно;
удаление подписки во время работы;
тесты без сети.
3. UdpSender
отправка готовых пакетов;
ошибки отправки;
тесты с TestSender.
4. DataServer
объединение всех компонентов;
полноценные интеграционные тесты.


публикация одной подписки;
публикация нескольких подписок;
разные частоты (1/10/100 Гц);
удаление подписки во время работы;
Publisher::publish() вернул false;
ISender::send() вызывается нужное число раз;
sequence увеличивается только после успешной отправки (это мы недавно поправили);
повторный tick() без наступления времени ничего не публикует.

После этого LiveScheduler можно будет считать законченным.