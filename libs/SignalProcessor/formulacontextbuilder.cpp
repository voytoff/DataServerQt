#include "formulacontextbuilder.h"

namespace qds
{
/*
FormulaContext FormulaContextBuilder::create(
  Frame& frame,
  const CalculationStep& step)
{
  m_inputs.clear();
  m_inputs.reserve(step.inputs.size());

  FormulaContext ctx;

  ctx.output =
    frame.address(step.output);

  for (const auto& ref : step.inputs)
  {
    m_inputs.push_back(
      frame.address(ref));
  }

  ctx.inputs = m_inputs;

  return ctx;
}
*/
}