#include "signaldefinition.h"

namespace qds
{
/*
bool SignalDefinition::isValid() const
{
  if (kind == SignalKind::Raw) {
    return id != 0 &&
           !std::empty(name) &&
           source.tag.value != 0;
  } else if (kind == SignalKind::Calculated) {
    return id != 0 &&
           !std::empty(name) &&
           formulaId != 0;
  }
  return false;
}
*/
}